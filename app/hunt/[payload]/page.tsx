'use client';

import React from 'react';
import HuntPage from '../page';

export default function PayloadHuntPage() {
  // Reuse the existing HuntPage component so /hunt/<code> works
  return <HuntPage />;
}
